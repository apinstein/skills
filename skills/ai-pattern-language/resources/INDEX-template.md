# [Domain Name] Pattern Language Index

This is the index of all patterns within the **[Domain Name]** domain. 
*Instructions for AI Agents:* Before creating a new pattern in this domain, check this index to determine the next available sequential ID (e.g., `P-001`, `B-042`). 
- **Naming Rule**: The pattern's filename must exactly start with its ID to act as a checksum (e.g., `P-001-frictionless-onboarding.md`).
Whenever you add or update a pattern in this directory, you **must** update this index to reflect the change. Include a 1-sentence summary of each pattern.

Note that the ID of the pattern should be reflected in:
- the pattern filename (e.g. `P-001-frictionless-onboarding.md`)
- the pattern's `[ID]` field
- the INDEX's pattern list (e.g. `[[ID]: [Pattern Name]]([filename].md)`)

## Patterns
- [[ID]: [Pattern Name]]([filename].md): [Insert 1-sentence summary of what this pattern dictates.]
- ...
