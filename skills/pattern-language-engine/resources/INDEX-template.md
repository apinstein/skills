# [Domain Name] Pattern Language Index

This is the index of all patterns within the **[Domain Name]** domain. A pattern language has the structure of a network. When using the network, always use it as a sequence, moving from larger patterns to smaller ones (from structural to those that embellish structures).

*Instructions for AI Agents:* 
1. Check this index to determine the next available sequential ID (e.g., `P-001`, `B-042`).
2. Order patterns by spatial scale from largest (most foundational) to smallest (most specific implementation detail). Reading "up" and "down" gives you a sense of which ones are needed.
3. **Naming Rule**: The pattern's filename must exactly start with its ID to act as a checksum (e.g., `P-001-frictionless-onboarding.md`).
4. Whenever you add or update a pattern in this directory, update this index to reflect the change. Include a 1-sentence summary of each pattern.

Note that the ID of the pattern should be reflected in:
- the pattern filename (e.g. `P-001-frictionless-onboarding.md`)
- the pattern's `[ID]` field
- the INDEX's pattern list (e.g. `[[ID]: [Pattern Name]]([filename].md)`)

## Patterns
- [[ID]: [Pattern Name]]([filename].md): [Insert 1-sentence summary of what this pattern dictates.]
- ...
